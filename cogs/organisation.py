"""
Legacy organisation cog removed.

This stub exists so that old imports fail fast with a clear message rather
than silently loading the deprecated workflow. Use the new `organisation`
module at the repository root instead.
"""

raise ImportError(
    "The legacy 'cogs.organisation' module has been removed. "
    "Please load the new 'organisation' module instead."
)
